# Architecture

This project uses a three-part layout:

- `frontend/` for the clinician dashboard
- `backend/` for REST and WebSocket APIs
- `ml/` for feature engineering, model training, and explainability
